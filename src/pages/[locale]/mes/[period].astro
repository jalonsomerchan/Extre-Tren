---
import DataPage from '../../../components/DataPage.astro';
import { defaultLocale, locales, type Locale } from '../../../config/site';
import { getAvailableMonths } from '../../../data/delays';

export function getStaticPaths() {
  return locales.filter((locale) => locale !== defaultLocale)
    .flatMap((locale) => getAvailableMonths().map((period) => ({ params: { locale, period } })));
}

const locale = Astro.params.locale as Locale;
const period = Astro.params.period!;
---

<DataPage section="month" locale={locale} period={period} />
